version: "3.9"
services:
  api:
    build: .
    container_name: oldmarket-api
    env_file:
      - .env.docker
    ports:
      - "8080:8080"
    restart: unless-stopped
    # Nếu dùng Mongo Atlas, không cần network đặc biệt
    # Nếu dùng mongo local compose, bỏ comment phần depends_on
    # depends_on:
    #   - mongo

  # Tùy chọn: MongoDB local (comment lại nếu dùng Atlas)
  # mongo:
  #   image: mongo:7
  #   container_name: oldmarket-mongo
  #   restart: unless-stopped
  #   environment:
  #     MONGO_INITDB_ROOT_USERNAME: root
  #     MONGO_INITDB_ROOT_PASSWORD: example
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongo_data:/data/db

volumes:
  mongo_data:
